import {
  LitElement,
  css,
  customElement,
  html,
  property,
  state
} from "./chunk-V5TVPMZB.js";
import {
  __decorateElement,
  __decoratorStart,
  __publicField,
  __runInitializers
} from "./chunk-JI5TOGPD.js";

// node_modules/.pnpm/@kodco+ui@file+ui/node_modules/@kodco/ui/src/blocks/schedule-block.ts
var DAY_ORDER = ["mon", "tue", "wed", "thu", "fri", "sat", "sun"];
var DEFAULT_DAY_LABELS = {
  mon: "Måndag",
  tue: "Tisdag",
  wed: "Onsdag",
  thu: "Torsdag",
  fri: "Fredag",
  sat: "Lördag",
  sun: "Söndag"
};
var _selectedDay_dec, _dayLabels_dec, _classes_dec, _emptyMessage_dec, _heading_dec, _a, _ScheduleBlock_decorators, _init;
_ScheduleBlock_decorators = [customElement("schedule-block")];
var ScheduleBlock = class extends (_a = LitElement, _heading_dec = [property({ type: String })], _emptyMessage_dec = [property({ type: String })], _classes_dec = [property({ type: Array })], _dayLabels_dec = [property({ type: Object })], _selectedDay_dec = [state()], _a) {
  constructor() {
    super(...arguments);
    __publicField(this, "heading", __runInitializers(_init, 8, this, "")), __runInitializers(_init, 11, this);
    __publicField(this, "emptyMessage", __runInitializers(_init, 12, this, "Inga klasser denna dag.")), __runInitializers(_init, 15, this);
    __publicField(this, "classes", __runInitializers(_init, 16, this, [])), __runInitializers(_init, 19, this);
    __publicField(this, "dayLabels", __runInitializers(_init, 20, this, DEFAULT_DAY_LABELS)), __runInitializers(_init, 23, this);
    __publicField(this, "selectedDay", __runInitializers(_init, 24, this, "mon")), __runInitializers(_init, 27, this);
  }
  get classesByDay() {
    const map = {};
    DAY_ORDER.forEach((d) => map[d] = []);
    this.classes.forEach((c) => {
      if (map[c.day]) map[c.day].push(c);
    });
    return map;
  }
  get visibleDays() {
    return DAY_ORDER.filter((d) => {
      var _a2;
      return (((_a2 = this.classesByDay[d]) == null ? void 0 : _a2.length) ?? 0) > 0;
    });
  }
  get currentClasses() {
    return this.classesByDay[this.selectedDay] ?? [];
  }
  connectedCallback() {
    super.connectedCallback();
  }
  updated(changed) {
    if (changed.has("classes")) {
      const firstVisible = this.visibleDays[0];
      if (firstVisible && !this.visibleDays.includes(this.selectedDay)) {
        this.selectedDay = firstVisible;
      }
    }
  }
  selectDay(day) {
    this.selectedDay = day;
  }
  onClassClick(item) {
    this.dispatchEvent(
      new CustomEvent("schedule:class-select", {
        detail: { class: item },
        bubbles: true,
        composed: true
      })
    );
  }
  dayLabel(dayId) {
    return this.dayLabels[dayId] ?? dayId;
  }
  render() {
    const visibleDays = this.visibleDays;
    const currentClasses = this.currentClasses;
    return html`
      <div class="container">
        ${this.heading ? html`<h2>${this.heading}</h2>` : ""}

        ${visibleDays.length > 0 ? html`
              <div class="pills-wrap">
                <div class="pills">
                  ${visibleDays.map(
      (day) => html`
                      <button
                        type="button"
                        class="pill ${this.selectedDay === day ? "active" : ""}"
                        @click=${() => this.selectDay(day)}
                      >
                        ${this.dayLabel(day)}
                      </button>
                    `
    )}
                </div>
              </div>

              ${currentClasses.length > 0 ? html`
                    <div class="grid">
                      ${currentClasses.map(
      (cls) => html`
                          <button
                            type="button"
                            class="card"
                            @click=${() => this.onClassClick(cls)}
                          >
                            <span class="time-bg" aria-hidden="true">${cls.time}</span>
                            <div class="card-content">
                              <h3 class="card-title">${cls.title}</h3>
                              <p class="card-meta">
                                ${cls.time} · ${cls.duration} min · ${cls.instructor}
                              </p>
                              <span class="card-cta">Boka min plats →</span>
                            </div>
                          </button>
                        `
    )}
                    </div>
                  ` : html`<p class="empty">${this.emptyMessage}</p>`}
            ` : html`<p class="empty">${this.emptyMessage}</p>`}
      </div>
    `;
  }
};
_init = __decoratorStart(_a);
__decorateElement(_init, 5, "heading", _heading_dec, ScheduleBlock);
__decorateElement(_init, 5, "emptyMessage", _emptyMessage_dec, ScheduleBlock);
__decorateElement(_init, 5, "classes", _classes_dec, ScheduleBlock);
__decorateElement(_init, 5, "dayLabels", _dayLabels_dec, ScheduleBlock);
__decorateElement(_init, 5, "selectedDay", _selectedDay_dec, ScheduleBlock);
ScheduleBlock = __decorateElement(_init, 0, "ScheduleBlock", _ScheduleBlock_decorators, ScheduleBlock);
__publicField(ScheduleBlock, "styles", css`
    :host {
      display: block;
      padding: 0 1rem 3rem;
      background: var(--color-background, #fafaf9);
    }

    .container {
      max-width: 72rem;
      margin: 0 auto;
    }

    h2 {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--color-text-primary, #1a1a1a);
      margin: 0 0 1rem 0;
    }

    @media (min-width: 640px) {
      h2 {
        font-size: 1.875rem;
      }
    }

    /* Day pills */
    .pills-wrap {
      position: sticky;
      top: 0;
      z-index: 20;
      margin: 0 -1rem;
      border-bottom: 1px solid color-mix(in srgb, var(--color-primary, #8f9779) 15%, transparent);
      background: color-mix(in srgb, var(--color-surface, #faf9f6) 90%, transparent);
      backdrop-filter: blur(12px);
    }

    .pills {
      display: flex;
      gap: 0.25rem;
      overflow-x: auto;
      padding: 1rem;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
      -webkit-overflow-scrolling: touch;
    }

    .pills::-webkit-scrollbar {
      display: none;
    }

    @media (min-width: 768px) {
      .pills {
        flex-wrap: wrap;
        padding-bottom: 1.25rem;
      }
    }

    .pill {
      flex-shrink: 0;
      scroll-snap-align: start;
      padding: 0.5rem 1.25rem;
      border: none;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.2s, color 0.2s;
      background: transparent;
      color: var(--color-text-secondary, rgba(26, 26, 26, 0.8));
    }

    .pill:hover {
      color: var(--color-text-primary, #1a1a1a);
    }

    .pill.active {
      background: var(--color-primary, #8f9779);
      color: var(--color-primary-contrast, #fff);
    }

    /* Class grid */
    .grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1rem;
      padding-top: 2.5rem;
    }

    @media (min-width: 640px) {
      .grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (min-width: 1024px) {
      .grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    .card {
      position: relative;
      min-height: 200px;
      padding: 1.5rem;
      border: none;
      border-radius: 1rem;
      text-align: left;
      cursor: pointer;
      background: color-mix(in srgb, var(--color-surface, #fff) 60%, transparent);
      box-shadow: 0 2px 20px color-mix(in srgb, var(--color-primary, #8f9779) 6%, transparent);
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .card:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 28px color-mix(in srgb, var(--color-primary, #8f9779) 12%, transparent);
    }

    .card::before {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: inherit;
      background: linear-gradient(
        160deg,
        color-mix(in srgb, var(--color-primary, #8f9779) 6%, transparent) 0%,
        color-mix(in srgb, var(--color-surface, #faf9f6) 40%, transparent) 100%
      );
      opacity: 0;
      transition: opacity 0.3s;
      pointer-events: none;
    }

    .card:hover::before {
      opacity: 1;
    }

    .time-bg {
      position: absolute;
      right: 0.5rem;
      top: 0.5rem;
      font-size: clamp(3rem, 10vw, 5.5rem);
      font-weight: 300;
      line-height: 1;
      color: color-mix(in srgb, var(--color-primary, #8f9779) 18%, transparent);
      pointer-events: none;
    }

    .card-content {
      position: relative;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      min-height: 100%;
    }

    .card-title {
      font-size: clamp(1.15rem, 2vw, 1.5rem);
      font-weight: 600;
      color: var(--color-text-primary, #1a1a1a);
      margin: 0 0 0.5rem 0;
      line-height: 1.25;
    }

    .card-meta {
      font-size: 0.75rem;
      color: var(--color-text-secondary, rgba(26, 26, 26, 0.6));
      margin: 0 0 0.75rem 0;
    }

    .card-cta {
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--color-primary, #8f9779);
      opacity: 0;
      transition: opacity 0.25s;
    }

    .card:hover .card-cta {
      opacity: 1;
    }

    .empty {
      padding: 4rem 1rem;
      text-align: center;
      font-size: 1.125rem;
      color: var(--color-text-secondary, rgba(26, 26, 26, 0.5));
    }
  `);
__runInitializers(_init, 1, ScheduleBlock);
export {
  ScheduleBlock
};
//# sourceMappingURL=@kodco_ui_src_blocks_schedule-block.js.map
